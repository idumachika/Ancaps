import React from 'react';

const MenuButton = (props) => {
    return (
        <div className="menuButton">
            <p >{props.name}</p>
        </div>
        
    );
}

export default MenuButton;